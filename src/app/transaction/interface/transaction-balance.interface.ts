export interface TransactionBalanceInterface {
    amount_inbound: number;
    amount_outbound: number;
    balance: number;
    inbound_to_month: number;
    outbound_to_month: number;
    balance_to_month: number;
}